{
  "name": "stream-hash",
  "version": "1.0.2",
  "description": "A transform stream that calls back with a hash",
  "repository": "juliangruber/stream-hash",
  "license": "MIT",
  "devDependencies": {
    "tap": "^9.0.0"
  },
  "scripts": {
    "test": "make test"
  }
}
